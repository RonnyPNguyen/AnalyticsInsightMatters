import React from "react";
import * as d3 from "d3";

const testData = [
	{ source: "A", target: "B", value: 10 },
	{ source: "A", target: "C", value: 20 },
	{ source: "B", target: "D", value: 30 },
	{ source: "C", target: "D", value: 40 },
];

const GraphSankey = (data) => {
	const nodes = [];
	const links = [];

	return <div></div>;
};

export default GraphSankey;
